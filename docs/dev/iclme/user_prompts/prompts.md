now look at the docker compose of wordpress located inside the vendor folder
and from there copy it inside the main app docker-compose (under awkn-0s1/) and also the app folder (in expected output: this one you do not copy you just ensure it's rewired accordingly in the docker compose)
after that run the script in the wordpress folder to install new plugins for wordpress
when it's done run the docker compose file for the app
and provide the urls to see each app

show an implementation plan 