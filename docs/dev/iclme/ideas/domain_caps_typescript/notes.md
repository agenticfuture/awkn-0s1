# Notes

- Corrected the scaffold so `domains/` lives directly under `awkn-0s1`.
- Used `project_name` as the example domain name placeholder.
- Used `messaging` as the example capability to show the full cap layout.
- Added `admin/` as a placeholder admin domain.
