# Notes

- Idea created from the request to scaffold the initial `docs/dev` structure.
- This folder follows the standard idea convention:
  - `prompts.md` for the original request
  - `notes.md` for context and decisions
  - `changes/` for implementation-specific follow-up
